<!--pages/my-records/my-records.wxml-->
<view class="container record-list">
  <block wx:for="{{records}}" wx:for-item="record">
    <text class="record-item">{{index + 1}}. {{record.created_at}}</text>
  </block>
</view>

